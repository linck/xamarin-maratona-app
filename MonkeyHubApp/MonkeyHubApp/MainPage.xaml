<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MonkeyHubApp"
             x:Class="MonkeyHubApp.MainPage"
             Title="Página Inicial">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="About" Command="{Binding AboutCommand}"/>
    </ContentPage.ToolbarItems>

    <StackLayout Padding="10, 30, 10, 10">
        <Entry Text="{Binding SearchTerm}"/>
        <Button Text="Pesquisar" Command="{Binding SearchCommand}"/>
        <ListView ItemsSource="{Binding Resultados}" ItemSelected="ListView_OnItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!--- O Contexto de Biding de um DataTemplate é o Binding da tag pai. Ex: Resultados-->
                    <TextCell Text="{Binding Name}" Detail="{Binding Description}"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>


</ContentPage>